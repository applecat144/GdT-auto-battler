Creating a new rangePhase() function aiming at solving the issue that
a missile unit's target might be already dead when it gets to fire.

unitFac :
    - remove meTarget and diTarget because they're useless now;

armyModule :
    - subsequently, removes missileTarget and meleeTarget methods;

rangePhase :
    - creates missileVolley(), that makes one missile unit rolls against its targets and apply wounds
    and route accordingly. Currently units automatically route if they get critted without rolling for
    morale. This needs fixing through armyModule.route();

Delete toBattle();