unitFac : 
    - add diTarget to returned object;
    - add meTarget to returned object;

create : 
    - resetArmyA;
    - woundUnit;
    - killUnit;

Modify unitFac : changed ID property to inID to avoid potential future conflicts;
Modify addUnit : use a simpler syntax to assign unit to the right category (remove switch statement);
Modify getUnitIndexById : use a simpler syntax to access the category of the unit type (remove switch statement);