create : 
    - unitFac : isEngaged property, engage and disengage methods;
    - armyAmodule.route;
    - armyAmodule.unroute;
    - armyAmodule.engageUnit;
    - armyAmodule.disengageUnit;

addUnit :
    - add army identifier in front of new units ID to prevent future
    potential conflicts;

getUnitIndexById : 
    - modify to aknowledge new units IDs;
    - added condition to return -1 when ID type is empty;
    - fix a bug where the function would return immediately after
    not finding the good unit for one time;